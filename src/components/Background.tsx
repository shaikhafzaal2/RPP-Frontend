import styled from 'styled-components';

const Background = styled.div`
  background: #ffffff;
  color: #ffffff;
  min-height: 100vh;
  overflow: hidden;
  position: relative;

  
`;

export default Background;
